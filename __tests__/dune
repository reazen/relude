(include_subdirs unqualified)

(melange.emit
 (target output)
 (alias test)
 (libraries melange melange-jest.jest melange.node bastet relude)
 (preprocess
  (pps melange.ppx))
 (module_systems commonjs))
